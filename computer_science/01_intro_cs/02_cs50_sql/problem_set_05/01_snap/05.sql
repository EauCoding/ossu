SELECT FRIEND_ID
FROM FRIENDS
WHERE USER_ID = (
    SELECT ID
    FROM USERS
    WHERE USERNAME = 'lovelytrust487'
)

INTERSECT

SELECT FRIEND_ID
FROM FRIENDS
WHERE USER_ID = (
    SELECT ID
    FROM USERS
    WHERE USERNAME = 'exceptionalinspiration482'
)
;
